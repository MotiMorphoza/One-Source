# OUTSIDE PROJECT RELATED CONTEXT

This repo currently contains enough structure that most important decisions can be taken from code instead of memory.

## Still Useful External Context

- keep the project lightweight
- keep it browser-first
- prefer patching over rewriting
- treat historical docs as context, not truth

## Current Repo Reminder

If outside notes mention `index.json`, treat that as future direction only. The current repo uses `hubIndex.js`.
