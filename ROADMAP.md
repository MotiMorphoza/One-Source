# ROADMAP

## Must Fix Now

1. Clean up dead import paths in the shell.
2. Improve service worker asset/version discipline.
3. Decide whether export should enforce the strict comma policy or document the current CSV behavior clearly.

## Later

1. Replace `hubIndex.js` with generated metadata if desired.
2. Normalize the bundled hub model into a cleaner topic abstraction.
3. Reduce shell rerenders if content volume grows.
